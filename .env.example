# Local dev: a file URL pointing to a SQLite file in /data
# (the /data directory is gitignored).
DATABASE_URL="file:./data/salary.db"
DATABASE_AUTH_TOKEN=""

# Production (Turso):
# DATABASE_URL="libsql://<your-db>.turso.io"
# DATABASE_AUTH_TOKEN="<your-token>"
